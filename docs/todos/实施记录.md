# 实施记录

按《消息通道扩展实施待办清单》§8.3 模板逐任务追加记录。所有凭证与敏感地址均已脱敏。

> 历史记录保留当时的实现与验证结论；其中 WxPusher 响应结构、API 推送覆盖和验收勾选存在偏差，已在末尾 [2026-09-20 复核修复记录](#2026-09-20-复核修复记录) 中更正，当前状态以该记录与待办清单为准。

---

任务编号 / Type / 模式：PRE-01 / — / —
实施状态 / 日期：已完成（1 项子条目待推送验证）/ 2026-09-19
关联提交或 PR：基线提交 `0179ce1`（master，远程 origin=github.com/seamusic/pusher）
官方依据 / 核验日期 / 上游版本（适用时）：不适用
本次改动与涉及路径：
- 修正 `.github/workflows/ci.yml`：触发路径由失效的 `dotnet/**` 改为 `src/**`、`tests/**`、`MessagePusher.slnx`、`Directory.Build.props`、workflow 自身；移除 `working-directory: dotnet`；构建/测试命令显式指向 `MessagePusher.slnx`（与清单 §8.1 一致）。
自动化命令 / 结果 / 报告位置：
- `dotnet --version` → 10.0.401（.NET 10 SDK 就绪）
- `dotnet build MessagePusher.slnx -c Release` → 0 错误、6 警告（既有，含 NU1903：`SQLitePCLRaw.lib.e_sqlite3` 2.1.10 已知高危漏洞，非本次新增，留待独立处理）
- `dotnet test MessagePusher.slnx --no-build -c Release` → 14/14 通过（Domain 1、Application 7、Api.Integration 6），无既有失败
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非通道任务）
兼容性回归与结果：基线即全绿，无需区分新增回归
原方案 / README / 数量更新位置：无改动
剩余问题 / 阻塞条件 / 下一步：
- CI 触发验收（提交仅触及 `src/**` 的变更并观察 workflow 运行）需在推送后进行；推送需用户确认，暂记为待验证。本地 `ci.yml` 语法与路径已按最终目录修正。
- 基线已实现 16 个 Type 确认：`email, test, corp_app, corp, lark, lark_app, ding, bark, client, telegram, discord, one_bot, group, custom, tencent_alarm, none`（Constants.cs + DI 16 个 Provider 注册一致）。
- 受影响旧行为样本清单（供 F/BUG 任务回归）：API snake_case 契约（JsonDefaults=SnakeCaseLower）、GroupProvider 循环内改写共享 `message.To/Channel`（SignedProviders.cs:355-363，顺序发送）、旧 Provider 经 `HttpChannelHelpers.PostJsonAsync/PostRawAsync` 发送（无 PostFormAsync）、配置更新 Secret 留空保留旧值、TokenStore 仅 test/corp_app/lark_app 三类且 `UpdateChannelAsync` 按旧 Type 早退、历史 `Other` 为标量/模板字符串、异步恢复为先回灌后消费（有界 128、Wait、静态 `_loaded` 守卫、重复 HostedService 注册）。
- 下一步：F-01。

---

任务编号 / Type / 模式：F-01 / group（行为修复）/ —
实施状态 / 日期：已完成 / 2026-09-19
关联提交或 PR：见本次 F-01 提交（基于基线 `0179ce1`）
官方依据 / 核验日期 / 上游版本（适用时）：不适用（内部改造）
本次改动与涉及路径：
- `src/MessagePusher.Domain/Entities/Message.cs`：新增 `Clone()`（全部属性为字符串/值类型，浅拷贝即独立副本）。
- `src/MessagePusher.Application/Channels/SignedProviders.cs`：GroupProvider 循环内不再改写共享 `message.To/Channel`，改为每个子通道 `message.Clone()` 后填写副本；嵌套群组拒绝、数量不一致报错、顺序发送等原有行为保留。
- 新增 `tests/MessagePusher.Application.Tests/GroupProviderTests.cs`（fake 仓储/Provider/作用域）、`tests/MessagePusher.Domain.Tests/MessageTests.cs`（Clone 全字段与独立性）。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release` → 21/21 通过（Domain 2、Application 13、Api.Integration 6）
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用（行为回归由自动化覆盖）
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非新增通道）
兼容性回归与结果：原有 13 项测试全部通过；Group 对外行为差异仅为"原消息不再被篡改"，即本任务目标；PushService 发送后使用的 `message.Id/Link/Async` 不受影响。
原方案 / README / 数量更新位置：无（Type 数量不变）
剩余问题 / 阻塞条件 / 下一步："新增 Provider 不改传入 Message"为编码约束，在 P0 各通道任务中落实并验收。下一步：F-02。

---

任务编号 / Type / 模式：F-02 / —（HTTP 基础能力）/ —
实施状态 / 日期：已完成 / 2026-09-19
关联提交或 PR：见本次 F-02 提交
官方依据 / 核验日期 / 上游版本（适用时）：不适用（内部改造）
本次改动与涉及路径：
- `src/MessagePusher.Application/Channels/HttpChannelHelpers.cs`：
  - 新增 `PostFormAsync`（FormUrlEncodedContent，支持自定义 Header），未新增 `PostTextAsync`；
  - `PostRawAsync` 补读取失败时释放 resp（原泄漏路径）；所有权规则以注释固定：读取失败 helper 释放，正常返回调用方释放；
  - 新增高层 `SendAndReadAsync`：resp 全程由 helper 持有释放；非 2xx 在业务反序列化前转可读 `BusinessException`（`BuildHttpError`：HTML 剥离为单行纯文本、截断 200 字符、按 sensitiveValues 脱敏）；业务 code/纯文本成功语义留给 Provider；无重试；
  - 新增 `OutboundUrlPolicy`（默认仅 HTTPS，`ChannelUrlAllowNonHttps`/同名环境变量开启后允许 HTTP，拒绝非 http(s) 绝对地址，禁止指向 ServerAddress；`WithDefault` 供 Provider 补默认服务器地址）与 `SecretMask`（`Redact` 原样替换凭证、`MaskKey` 日志短掩码）。
- `src/MessagePusher.Application/Channels/SimpleProviders.cs`：CustomProvider 内联 URL 检查迁移至 `OutboundUrlPolicy`（拒绝行为不变，新增拒绝非 http(s) 协议，属有意收紧）。
- `src/MessagePusher.Application/DependencyInjection.cs`：`channels` 客户端超时归口为 30 秒（原默认 100 秒）；按请求仍可用 ct 收紧，新通道不得另设客户端级 Timeout。
- `src/MessagePusher.Application/MessagePusher.Application.csproj`：`InternalsVisibleTo` 开放给 Application.Tests。
- 新增 `tests/MessagePusher.Application.Tests/HttpChannelHelpersTests.cs`（StubHandler/ThrowingStream/FakeOptions）。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release` → 35/35 通过（Domain 2、Application 27、Api.Integration 6）
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非新增通道）
兼容性回归与结果：旧 helper（PostJsonAsync/PostRawAsync）签名与默认序列化行为未变；CustomProvider 拒绝行为回归由 URL 策略测试覆盖；全部既有测试通过。已知差异两处并记录：①CustomProvider 对非 http(s) 协议由放行改为拒绝；②`channels` 客户端超时上限由 100s 收紧为 30s。
原方案 / README / 数量更新位置：无（Type 数量不变）
剩余问题 / 阻塞条件 / 下一步：超时/取消、脱敏、重试口径已固化，P0 各 Provider 直接复用 `SendAndReadAsync`+`OutboundUrlPolicy`。下一步：F-03。

---

任务编号 / Type / 模式：F-04 / —（序列化隔离）/ —
实施状态 / 日期：已完成（与 F-02 同批）/ 2026-09-19
关联提交或 PR：见本次 F-04 提交
官方依据 / 核验日期 / 上游版本（适用时）：不适用（内部改造；协议字段名样例取自原方案 §4.4 WxPusher）
本次改动与涉及路径：
- 新增 `src/MessagePusher.Application/Json/OutboundJson.cs`：出站专用 `JsonSerializerOptions`（无命名策略，字段名由 `[JsonPropertyName]` 决定；响应解析大小写不敏感）。`JsonDefaults` 未改动。
- `src/MessagePusher.Application/Channels/HttpChannelHelpers.cs`：`PostJsonAsync`/`PostRawAsync` 增加可选 `options` 参数（缺省仍为 JsonDefaults，旧调用零影响）；新增 `JsonContent(body, options)` 便利方法供 `SendAndReadAsync` 路径构造出站 JSON。
- 新增 `tests/MessagePusher.Application.Tests/SerializationIsolationTests.cs`。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release` → 40/40 通过（Domain 2、Application 32、Api.Integration 6）
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非新增通道）
兼容性回归与结果：最终发送字节断言 `appToken/contentType/topicIds` 正确、无 `app_token/content_type/topic_ids` 错名；对外 API snake_case 契约（ApiResult、JsonDefaults）与旧 Provider 默认序列化（`chat_id/parse_mode`）回归通过；全局命名策略未被修改。
原方案 / README / 数量更新位置：无（Type 数量不变）
剩余问题 / 阻塞条件 / 下一步：P0-03 PushPlus 与 P1-04 WxPusher 的正式 DTO 随通道任务落地并复用本配置。下一步：F-03。

---

任务编号 / Type / 模式：F-03 / —（配置校验入口）/ —
实施状态 / 日期：已完成 / 2026-09-19
关联提交或 PR：见本次 F-03 提交
官方依据 / 核验日期 / 上游版本（适用时）：不适用（内部改造）
本次改动与涉及路径：
- 新增 `src/MessagePusher.Application/Channels/ChannelConfigValidators.cs`：`IChannelConfigValidator`（按 Type 注册，校验必填项/目标/模式/URL/取值/互斥）、`ChannelConfigValidatorRegistry`、`ChannelTargetPolicy`（动态目标 To 优先；固定目标非空 To 明确业务错误）。
- `src/MessagePusher.Application/Services/ChannelService.cs`：注入 `ChannelProviderFactory` + 校验器注册表；新增与完整更新路径均先合并后校验（Type 必须为已注册 Provider，未知 Type/未实现候选拒绝保存）；同 Type 下 Secret 留空保留旧值，跨 Type 一律取新输入（不盲用旧密钥）；完整更新路径补 Name 长度/保留名校验；statusOnly 单独处理（不要求凭证、跳过配置校验）；返回配置继续清空 Secret。
- `src/MessagePusher.Application/Tokens/TokenStore.cs`：修复 `UpdateChannelAsync` 跨 Type 编辑——旧条目移除与新条目添加相互独立，仅同 Type 沿用"留空保留旧凭证"合并，跨 Type 用新凭证原样构建，防止旧密钥误填充新类型条目。
- `src/MessagePusher.Application/DependencyInjection.cs`：注册 `ChannelConfigValidatorRegistry`。
- 新增 `tests/MessagePusher.Application.Tests/ChannelConfigValidationTests.cs`（ChannelService 校验 8 项、目标策略 2 项、TokenStore 跨 Type 4 项）。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release` → 54/54 通过（Domain 2、Application 46、Api.Integration 6）
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非新增通道）
兼容性回归与结果：API 集成测试全部通过；Secret 留空保留、状态更新不要求密码、返回不回显等旧行为保留。已知有意收紧两处：①完整更新与新增路径此前不校验 Type 合法性，现拒绝未注册 Type；②跨 Type 更新不再沿用旧 Secret。历史 `Other`（custom 模板、corp 的 clientType、tencent_alarm 的 region 等标量语义）未强制改 JSON。
原方案 / README / 数量更新位置：无（Type 数量不变）
剩余问题 / 阻塞条件 / 下一步：各新通道的具体校验器与发送前 `ChannelTargetPolicy` 调用随 P0/P1 通道任务落地。下一步：BUG-01。

---

任务编号 / Type / 模式：BUG-01 / —（异步恢复缺陷）/ —
实施状态 / 日期：已完成 / 2026-09-19
关联提交或 PR：见本次 BUG-01 独立提交
官方依据 / 核验日期 / 上游版本（适用时）：不适用（内部缺陷，原方案 §6.5）
本次改动与涉及路径：
- **复现结果（修复前）**：`Startup_recovery_of_more_than_queue_capacity_completes`（129 条待恢复、有界 128/Wait、fake 仓储/Provider、10 秒有界等待）在修复前失败——`condition not met within 10000ms`，0 条被消费：旧实现 `ExecuteAsync` 先 `await LoadPendingAsync` 再进入消费循环，第 129 条回灌等待队列空位而消费者未启动，永久阻塞。
- `src/MessagePusher.Application/Messaging/AsyncMessageWorker.cs`：回灌改为与消费并发推进（`Task.Run(LoadPendingAsync)` + 消费循环同时进行）；停机时 finally 等待回灌任务，取消（OperationCanceledException）吞掉、其他异常记录日志；移除静态 `_loaded` 守卫（重复注册的伴生补丁，静态状态跨实例/跨测试泄漏）。
- `src/MessagePusher.Application/DependencyInjection.cs`：删除重复的 `AddHostedService<AsyncMessageWorker>()` 注册，单消费者；无多消费者需求，不做显式多实例配置。
- `src/MessagePusher.Application/Messaging/AsyncMessageQueue.cs`：**未改动**——保持有界 128 + FullMode.Wait，未以扩容或无界队列掩盖阻塞。
- 新增 `tests/MessagePusher.Application.Tests/AsyncMessageWorkerTests.cs`。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release` → 61/61 通过（Domain 2、Application 53、Api.Integration 6）。回归覆盖：待恢复 0/128/129/256 条全部进入消费并完成；5000 条回灌期间 StopAsync 不挂起；回灌异常不阻塞后续消费；重启后新实例重新回灌（静态守卫已除）；`Distinct` 断言不因回灌/注册重复发送同一条。
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用（行为由自动化覆盖）
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非通道任务）
兼容性回归与结果：API 集成测试（含异步发送路径）全部通过；对外行为差异仅为"超过 128 条待恢复消息时不再永久阻塞"与"单一消费者"。
原方案 / README / 数量更新位置：无（Type 数量不变）
剩余问题 / 阻塞条件 / 下一步：本修复不承诺跨进程故障下严格一次送达（进程崩溃时已入队未消费的消息仍靠下次启动回灌恢复），未引入消息中间件。下一步：P0 三通道。

---

任务编号 / Type / 模式：P0-01 / `server_chan` / Turbo + Server酱³（同一 Type，双模式）
实施状态 / 日期：编码与自动化完成，真实联调待条件 / 2026-09-19
关联提交或 PR：见本次 P0-01 独立提交
官方依据 / 核验日期 / 上游版本（适用时）：2026-09-19 复核
- serverchan-sdk README（github.com/easychen/serverchan-sdk）：字段 title/desp，options tags(SC3专用)/short(SC3+SCT)/noip(SCT)/channel(SCT)/openid(SCT)；返回 `{code(0为正确),message,data?}`，data 两版本不同构。
- serverchan-sdk-golang/serverchan.go（官方实现，master）：路由 `^sctp(\d+)t` → `https://<uid>.push.ft07.com/send/<key>.send`，否则 → `https://sctapi.ftqq.com/<key>.send`；非法 sctp 请求前报错；官方 SDK 用 JSON body（`application/json;charset=utf-8`）。
- Turbo 标题上限 32 字符（Server酱 Turbo 文档）；SC3 标题上限未获官方明确数值——标记为未知，SC3 不做本地截断，交服务端判定。
本次改动与涉及路径：
- `src/MessagePusher.Domain/Constants.cs`：新增 `ChannelType.ServerChan = "server_chan"`。
- `src/MessagePusher.Application/Channels/ServerChanProvider.cs`（新增）：`ServerChanKey`（IsValid/IsSc3/ResolveEndpoint/ParseOptions，GeneratedRegex `^sctp(\d+)t`）、`ServerChanProvider`、`ServerChanResponse`、`ServerChanConfigValidator`。
- `src/MessagePusher.Application/Channels/HttpChannelHelpers.cs`：新增共享 `ComposeBody`（Content 空回退 Description，追加 Url 且不重复）、`TruncateUnicode`（按字素簇边界截断，不切代理项）。
- `src/MessagePusher.Application/DependencyInjection.cs`：注册 `ServerChanProvider` 与 `ServerChanConfigValidator`。
- 前端 `src/MessagePusher.Web/Api/Models.cs`（下拉项）、`Components/Pages/ChannelEdit.razor`（Fields：SendKey/channel/高级选项 JSON；Hint：Key 不通用、额度以官网为准、官方链接）。
- 新增 `tests/MessagePusher.Application.Tests/ServerChanProviderTests.cs`（23 项）。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release --filter FullyQualifiedName~ServerChan` → 24/24 通过；全量 `dotnet test MessagePusher.slnx -c Release` → 84/84（Domain 2、Application 76、Api 6）。覆盖：Turbo/SC3 端点路由、非法 Key 请求前报错（0 次调用）、表单编码与 Content-Type、Content 空回退、Url 不重复追加、Turbo 标题 32 字素边界不切代理项、Turbo AccountId→channel 且 To 覆盖、SC3 拒绝非空 To、short 双版本/tags 仅 SC3、业务 code!=0 报错、空响应报错、非 2xx HTML 转可读错误并脱敏 SendKey、输入消息不被修改；校验器：非法 Key/SC3 带 AccountId/Turbo 带 tags/Other 非法 JSON 均拒绝。
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：**待联调**——无 Turbo 与 Server酱³ 真实账号/凭证，G-05 真实发送、前端创建编辑测试、群组组合、异步发送均未在真实目标验证。
G-01～G-06 完成情况或不适用理由（通道任务）：G-01 已复核（见上）；G-02 完成（Type/Provider/校验器/DI/命名客户端 `channels`）；G-03 完成（下拉/字段/密码输入/Hint，未写死额度）；G-04 完成（自动化捕获最终请求 URL/Header/Content-Type/线上字段，含成功与失败样例，未做真实发送）；G-05 **待联调**（缺账号）；G-06 待 R-01 批次验收后统一更新数量/README。
兼容性回归与结果：既有 Provider 未改；`ComposeBody`/`TruncateUnicode` 为新增共享方法，未触碰旧序列化路径；全量回归绿。
原方案 / README / 数量更新位置：原方案 §4.1；README/数量待 R-01（G-06）统一更新，暂不预增支持数量。
剩余问题 / 阻塞条件 / 下一步：阻塞——缺 Server酱 真实账号，G-05 待联调；表单 POST 与官方 SDK 的 JSON POST 均被端点接受，本实现按待办清单采用表单，真实端点对表单/JSON 的接受度以联调为准。下一步：P0-02 pushdeer。

---

任务编号 / Type / 模式：P0-02 / `pushdeer` / 单模式（表单推送）
实施状态 / 日期：编码与自动化完成，真实联调待条件 / 2026-09-19
关联提交或 PR：见本次 P0-02 独立提交
官方依据 / 核验日期 / 上游版本（适用时）：2026-09-19 复核
- pushdeer.com/official.html：`POST /message/push`，`application/x-www-form-urlencoded`，字段 pushkey/text/desp/type，type 允许 text/markdown/image，默认端点 api2.pushdeer.com。
- easychen/pushdeer `api/app/Http/Controllers/PushDeerMessageController.php`（main）：校验 pushkey/text 必填、desp/type 可空，type 服务端缺省 `markdown`；pushkey 可逗号分隔多 Key。
- easychen/pushdeer `api/app/Http/Helpers.php`（main）：成功 `http_result($content,$code=0)` → `{"code":0,"content":{...}}`；失败 `send_error($error,$code='9999')` → `{"code":"<字符串>","error":"..."}`。故成功判定只认数值 0，错误码可能为字符串。
本次改动与涉及路径：
- `src/MessagePusher.Domain/Constants.cs`：新增 `ChannelType.PushDeer = "pushdeer"`。
- `src/MessagePusher.Application/Channels/PushDeerProvider.cs`（新增）：`PushDeerProvider`、`PushDeerResponse`（`JsonElement Code` 兼容数值/字符串码）、`PushDeerConfigValidator`；复用 `OutboundUrlPolicy.WithDefault/Validate`、`HttpChannelHelpers.ComposeBody/SendAndReadAsync`、`ChannelTargetPolicy.RejectFixedTargetTo`。
- `src/MessagePusher.Application/DependencyInjection.cs`：注册 `PushDeerProvider` 与 `PushDeerConfigValidator`。
- 前端 `Models.cs`（下拉项）、`ChannelEdit.razor`（Fields：PushKey/服务器地址/消息类型下拉；Hint：固定目标、默认端点、可自建、官方链接）。
- 新增 `tests/MessagePusher.Application.Tests/PushDeerProviderTests.cs`（18 项）。
自动化命令 / 结果 / 报告位置：`dotnet test --filter FullyQualifiedName~PushDeer` → 18/18；全量 `dotnet test MessagePusher.slnx -c Release` → 102/102（Domain 2、Application 94、Api 6）。覆盖：默认/自建端点与末尾斜杠、表单编码与 Content-Type、text←Title、desp 回退+Url、type 仅配置时下发、非空 To 请求前拒绝(0 调用)、中文&空格&=符号编码、成功 code0、字符串错误码失败、空响应失败、非 2xx HTML 脱敏 pushkey、http 自建默认拒绝/开关放行；校验器：空 pushkey/非法 type/http 无开关拒绝、text/markdown/空 type 通过。
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：**待联调**——无 PushDeer 账号与已绑定设备，G-05（默认/自建端点、前端仅 Title+Description 测试消息真实发送）未验证。
G-01～G-06 完成情况或不适用理由（通道任务）：G-01 已复核；G-02 完成（Type/Provider/校验器/DI/命名客户端 `channels`）；G-03 完成（下拉/字段/密码输入/类型下拉/Hint）；G-04 完成（捕获最终请求，含成功与失败样例，无真实发送）；G-05 **待联调**（缺账号）；G-06 待 R-01。
兼容性回归与结果：既有 Provider 未改；复用 F-02 共享方法；全量回归绿。
原方案 / README / 数量更新位置：原方案 §4.2；README/数量待 R-01（G-06）。
剩余问题 / 阻塞条件 / 下一步：阻塞——缺 PushDeer 账号，G-05 待联调；本期不做 Basic Auth（自建鉴权端点不支持），image 类型不在范围。下一步：P0-03 push_plus。

---

任务编号 / Type / 模式：P0-03 / `push_plus` / 单模式（JSON 推送，个人/群组 topic）
实施状态 / 日期：编码与自动化完成，真实联调待条件 / 2026-09-19
关联提交或 PR：见本次 P0-03 独立提交
官方依据 / 核验日期 / 上游版本（适用时）：2026-09-19 复核
- pushplus.plus/doc/guide/api.html：`POST https://www.pushplus.plus/send`，`application/json`，字段 token(必填)/title/content(必填)/template/topic/channel；`url` 非文档参数（用 callbackUrl），故 Url 追加进 content。成功 `{code:200,msg,data}`，code=200 为受理成功、投递异步。模板值较多且未穷举：html(默认)/txt/json/markdown/cloudMonitor/jenkins/route/pay/form/doc/excel 等。
- pushplus.plus/doc/guide/use.html：未实名返回 905（仅影响发送/支付）；未实名每日请求 0 次、频率约 1 分钟 5 次、相同内容 1 小时 3 条；标题 ≤100 字、内容 ≤20000 字。
本次改动与涉及路径：
- `src/MessagePusher.Domain/Constants.cs`：新增 `ChannelType.PushPlus = "push_plus"`。
- `src/MessagePusher.Application/Channels/PushPlusProvider.cs`（新增）：`PushPlusProvider`、`PushPlusRequest`（`[JsonPropertyName]` 固定 token/title/content/template/topic，可选项 `WhenWritingNull` 覆盖出站 Never）、`PushPlusResponse`、`PushPlusConfigValidator`（模板按标识符结构校验，不写死白名单）；序列化经 `OutboundJson.Options`，标题/内容按 100/20000 字素边界截断，topic 走 `ChannelTargetPolicy.ResolveDynamic`。
- `src/MessagePusher.Application/DependencyInjection.cs`：注册 `PushPlusProvider` 与 `PushPlusConfigValidator`。
- 前端 `Models.cs`（下拉项）、`ChannelEdit.razor`（Fields：token/topic/template；Hint：905 实名、频率与长度限制、code=200 为受理非送达、官方链接）。
- 新增 `tests/MessagePusher.Application.Tests/PushPlusProviderTests.cs`（18 项）。
自动化命令 / 结果 / 报告位置：`dotnet test --filter FullyQualifiedName~PushPlus` → 18/18；全量 `dotnet test MessagePusher.slnx -c Release` → 120/120（Domain 2、Application 112、Api 6）。覆盖：固定端点/方法/Content-Type、精确字段名且无 snake_case、可选字段空时不下发、topic 个人/群组与 To 覆盖、template 仅配置时下发、content 回退+Url、标题/内容截断至 100/20000 且不切代理项、code=905 业务失败带 msg、空响应失败、非 2xx HTML 脱敏 token、输入消息不被修改；校验器：空 token/非法模板名拒绝，html/markdown/cloudMonitor/空 通过。
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：**待联调**——无 PushPlus token 与实名账号，G-05（个人/群组真实发送、前端测试消息、905 真实返回）未验证。
G-01～G-06 完成情况或不适用理由（通道任务）：G-01 已复核；G-02 完成（Type/Provider/校验器/DI/命名客户端 `channels`/出站 JSON 配置）；G-03 完成（下拉/字段/密码输入/Hint，额度与实名说明，未把受理当送达）；G-04 完成（捕获最终 JSON 字节与精确字段名，含成功/失败样例，无真实发送）；G-05 **待联调**（缺 token）；G-06 待 R-01。
兼容性回归与结果：既有 Provider 未改；`OutboundJson` 隔离第三方字段名，项目对外 API 仍 snake_case（F-04 回归测试保持绿）。
原方案 / README / 数量更新位置：原方案 §4.3；README/数量待 R-01（G-06）。
剩余问题 / 阻塞条件 / 下一步：阻塞——缺 PushPlus 实名账号/token，G-05 待联调；模板白名单未穷举核验，采用结构化校验。下一步：R-01 P0 批次验收。

---

任务编号 / Type / 模式：R-01 / P0 批次验收 / —
实施状态 / 日期：部分完成，G-05 真实联调阻塞 / 2026-09-19
关联提交或 PR：e6582a2(P0-01)、1e8ce31(P0-02)、a333ae1(P0-03)；前置 7850051(BUG-01)、0133c46(F-03)、0df0714(F-04) 等
官方依据 / 核验日期 / 上游版本（适用时）：见各 P0 记录
本次改动与涉及路径：R-01 为验收任务，本条不引入产品代码改动，仅记录核对结果。
自动化命令 / 结果 / 报告位置：
- `dotnet build MessagePusher.slnx -c Release` → 已成功生成（含 Web/Blazor）。
- `dotnet test MessagePusher.slnx -c Release` → **120/120 通过**（Domain 2、Application 112、Api.Integration 6）。
- `git diff --check` → 无空白/冲突标记。
- 回归覆盖：Api 集成（含推送/异步路径）、GroupProvider 子通道隔离、ChannelConfigValidation（完整编辑/状态编辑/跨类型密钥/未知 Type 拒绝）、AsyncMessageWorker（0/128/129/256、回灌期停机、异常、重启二次回灌、无重复发送）、旧 Provider 行为（Bark/Telegram/Lark/Ding/Corp/序列化契约）。
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：**全部待联调**——三通道均缺真实账号/凭证（Server酱 Turbo 与 SC3、PushDeer 已绑定设备、PushPlus 实名 token），G-05 的真实发送、前端创建/编辑/测试消息、群组组合、异步发送、自建端点均未在真实目标验证。
G-01～G-06 完成情况或不适用理由（通道任务）：批次层面 G-01~G-04 三通道齐备；G-05 阻塞；G-06 未执行（数量/README 未从 16 增至 19，见下）。
兼容性回归与结果：既有 16 Type 行为未变；新增 3 Type 代码侧已注册（ChannelType 常量、Provider、校验器、前端下拉均为 19 项），项目对外 API 序列化契约保持 snake_case。
原方案 / README / 数量更新位置：**未更新**——README.md（第 9、58 行）、Channel.razor（第 11 行）、Login.razor（第 17 行）仍标 "16 种"。依据 G-06「验收后更新数量」与 R-01「三 Type 全部验收时数量为 19」，在 G-05 真实联调达成前不预增，避免把待联调项冒充已交付。
剩余问题 / 阻塞条件 / 下一步：
- **阻塞（外部条件）**：三通道 G-05 真实联调需真实账号，本环境无法自足完成。
- **待用户决策**：数量口径——(A) 以"已实现+自动化验证"为准，授权将 README/页面数量增至 19 并把 G-05 记为残余阻塞；或 (B) 维持 16，将三通道标注为"已实现待联调"，待用户提供账号完成 G-05 后再增至 19。
- 下一步：按用户口径收尾 R-01；若继续 P1，需先处理 PRE-01 遗留的 CI 触发核验（需一次 push，待用户确认）。

**R-01 补充（2026-09-19，用户决策后）**：用户确认数量口径为「已实现 + 自动化验证即计入支持数量，G-05 真实联调记为残余阻塞」。据此执行 G-06：README.md 第 9 行「16 种渠道」→「19 种渠道」、第 58 行「16 通道」→「19 通道」、第 14 行特性列表补 Server酱/PushDeer/PushPlus、类型清单补 `server_chan`/`pushdeer`/`push_plus`、字段支持表补三行并加 `to`/`url` 说明脚注；Channel.razor 与 Login.razor「16 种」→「19 种」。R-01「前端下拉/Fields/Hint、数量、README 一致」项已勾选。**残余阻塞不变**：三通道 G-05 真实发送联调仍待真实账号，未在真实目标验证；数量增至 19 表示代码交付口径，不代表已完成真实联调。下一步按用户选择：push 并验证 CI 触发（PRE-01 遗留项）。

---

任务编号 / Type / 模式：PRE-01（CI 触发验收补记）/ — / —
实施状态 / 日期：已完成 / 2026-09-19
关联提交或 PR：推送 master@a9819ac（含 PRE-01~R-01 共 11 个提交）
本次改动与涉及路径：无产品代码改动，仅记录 CI 触发验收结果。
自动化命令 / 结果 / 报告位置：`git push origin master` → `0179ce1..a9819ac`；GitHub Actions 查询 `repos/seamusic/pusher/actions/runs`：run **35453319400**，name `ci`，head_sha `a9819ac…`，event `push`，status `completed`，conclusion **success**。运行链接 https://github.com/seamusic/pusher/actions/runs/35453319400 。
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（PRE-01 非通道任务）。
剩余问题 / 阻塞条件 / 下一步：PRE-01 验收口径达成——仅触及 `src/**`/`tests/**` 的推送确实触发 `ci` 且 build+test 步骤实际执行并通过，路径/工作目录修正生效。R-01 唯一残余阻塞为三通道 G-05 真实联调（缺账号）。下一步：待用户提供账号完成 G-05，或按用户指示推进 P1。

---

任务编号 / Type / 模式：P1 核心批次（P1-01~P1-06）/ `ntfy`、`gotify`、`pushover`、`wx_pusher`(标准)、`pushme`；`xizhi` 阻塞 / 见各条
实施状态 / 日期：五通道编码与自动化完成、真实联调待条件；息知与 WxPusher SPT 协议待核验未开放 / 2026-09-20
关联提交或 PR：见本次 P1 各独立提交（Domain 常量 → 各 Provider+测试 → DI/前端注册 → README → 文档）
官方依据 / 核验日期 / 上游版本（适用时）：2026-09-20 复核
- ntfy：docs.ntfy.sh/publish（publish-as-json）——JSON 发往**服务器根路径**，请求 `{topic,title,message,click,priority,tags}`，成功返回已发布消息对象（含 id/time/event=message），错误经非 2xx + `{code,http,error}`；可选 `Authorization: Bearer <token>`。
- Gotify：gotify.net/docs/pushmsg——JSON `POST {server}/message`，Header `X-Gotify-Key` 承载 App Token，请求 `{title,message,priority}`，成功返回消息对象 `{id,appid,...}`，错误 `{error,errorCode,errorDescription}`。
- Pushover：pushover.net/api——表单 `POST https://api.pushover.net/1/messages.json`，字段 `token/user/message/title/url`，成功 `{status:1,request}`，失败经非 2xx + `{errors:[...]}`；title≤250、message≤1024、url≤512。
- WxPusher：wxpusher.zjiecode.com/docs——标准模式 JSON `POST /api/send/message`，字段 `appToken/content/summary/contentType/uids/topicIds/url`，成功 `code==1000`，`data` 含逐目标结果；SPT 专属端点/载荷公开文档未固化，**待核验未实现**。
- PushMe：push.i-i.me/docs——表单 `POST` 服务根地址，字段 `push_key/title/content/type`，成功返回**纯文本 `success`**，其余文本为错误；type 含 text/markdown 等，首版仅取 text/markdown。
本次改动与涉及路径：
- `src/MessagePusher.Domain/Constants.cs`：新增 `Ntfy/Gotify/Pushover/WxPusher/PushMe` 五个 Type 常量（xizhi 未开放，不加常量）。
- `src/MessagePusher.Application/Channels/NtfyProvider.cs`（新增）：`NtfyProvider`、`NtfyOptions`(priority 1~5 / tags 逗号串或数组)、`NtfyRequest`(可选字段 WhenWritingNull)、`NtfyResponse`(按 id/event=message 判成功)、`NtfyConfigValidator`。
- `src/MessagePusher.Application/Channels/GotifyProvider.cs`（新增）：`GotifyProvider`(X-Gotify-Key 头、/message、id>0 判成功)、`GotifyOptions`(priority 0~10)、`GotifyRequest`、`GotifyResponse`、`GotifyConfigValidator`(Url 必填)。
- `src/MessagePusher.Application/Channels/PushoverProvider.cs`（新增）：`PushoverProvider`(表单、status==1、截断 250/1024/512)、`PushoverResponse`、`PushoverConfigValidator`(token+userKey 必填)。
- `src/MessagePusher.Application/Channels/WxPusherProvider.cs`（新增）：`WxPusherProvider`(标准模式、To 覆盖仅用该 UID 且清空 topicIds、code==1000 后查 data.fails 部分失败)、`WxPusherOptions`(mode/contentType/topicIds，UID 容忍 `|`/逗号，mode=spt 拒绝)、`WxPusherRequest`(精确字段名)、`WxPusherResponse`(防御式 ExtractFails)、`WxPusherConfigValidator`。
- `src/MessagePusher.Application/Channels/PushMeProvider.cs`（新增）：`PushMeProvider`(表单根地址、仅纯文本 success 判成功、不 JSON 反序列化)、`PushMeConfigValidator`(Other 仅 text/markdown)。
- `src/MessagePusher.Application/DependencyInjection.cs`：注册五个 Provider 与五个校验器（xizhi 未注册）。
- 前端 `Models.cs`（下拉新增 5 项，共 24 项）、`ChannelEdit.razor`（Fields+Hint：ntfy topic/Bearer/服务器/高级选项、gotify AppToken/服务器/priority、pushover token/userKey、wx_pusher appToken/UID/高级选项、pushme push_key/服务地址/类型）。
- 新增测试 `NtfyProviderTests.cs`(23)、`GotifyProviderTests.cs`(16)、`PushoverProviderTests.cs`(13)、`WxPusherProviderTests.cs`(20)、`PushMeProviderTests.cs`(16)。
自动化命令 / 结果 / 报告位置：全量 `dotnet test MessagePusher.slnx -c Debug` → **208/208 通过**（Domain 2、Application 200、Api.Integration 6）；`dotnet build MessagePusher.slnx` → 0 错误（仅既有 SQLitePCLRaw NU1903 与既有测试 CS0642/xUnit1031 警告，本次新增代码无新警告）。覆盖要点：ntfy 根路径/To 覆盖/Bearer 头/click 独立/中文/成功消息对象/错误结构/自建 URL 与 http 开关；gotify /message+X-Gotify-Key/正文追加 url/priority 下发与省略/id>0 判成功/固定目标拒绝/Url 必填；pushover 表单字段/To 覆盖/message 回退+url 独立/截断/status!=1 失败/脱敏；wx_pusher 精确字段名无 snake_case/UID 拆分/To 覆盖清空 topicIds/contentType/summary 截断/code!=1000 失败/data.fails 部分失败/SPT 拒绝/脱敏；pushme 默认根地址/正文追加 url/type 仅配置时下发/固定目标拒绝/仅纯文本 success 判成功/JSON 串按错误/脱敏/http 开关。
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：**全部待联调**——五通道均缺真实账号/凭证或自建服务（ntfy 公开/受保护 topic、Gotify 自建服务、Pushover appToken+userKey、WxPusher appToken+UID、PushMe push_key+真机），G-05 真实发送、前端创建/编辑/测试消息、群组组合、异步发送均未在真实目标验证。
G-01～G-06 完成情况或不适用理由（通道任务）：五通道 G-01~G-04、G-06 齐备；G-05 阻塞（缺账号/自建服务）。WxPusher SPT 与 xizhi 因协议待核验，未开放 Type/未计入数量，明确记为阻塞，不凭历史样例补造实现。
兼容性回归与结果：既有 Provider 未改；复用 F-02 共享方法（ComposeBody/TruncateUnicode/SendAndReadAsync/OutboundUrlPolicy）与 F-04 出站序列化隔离；`OutboundJson` 保证第三方字段名，项目对外 API 仍 snake_case（既有回归绿）。
原方案 / README / 数量更新位置：原方案 §4.5/§4.6/§4.7/§4.4/§4.9；README.md 第 9 行「19 种渠道」→「24 种渠道」、第 15 行特性列表补 ntfy/Gotify/Pushover/WxPusher/PushMe、第 59 行「19 通道」→「24 通道」、类型清单补五个 Type、字段支持表补五行并扩 `to`/`url` 脚注（含固定目标、click/url 独立字段、SPT 与 xizhi 未开放说明）；Channel.razor 与 Login.razor「19 种」→「24 种」。
剩余问题 / 阻塞条件 / 下一步：
- **阻塞（外部条件）**：五通道 G-05 真实联调需真实账号/自建服务，本环境无法自足完成。
- **阻塞（协议核验）**：P1-05 xizhi 当前发送域名/编码/成功码无法确认，保留 P1 候选、不开放 Type；P1-04 WxPusher SPT 专属端点/载荷待核验，仅实现标准模式（仅计一个 Type）。
- 数量口径：按 R-01 既定「已实现+自动化验证即计入，G-05 记残余阻塞」，P0+P1 已实现五通道后数量为 **24**（非 25，因 xizhi 阻塞、WxPusher 仅计一个 Type）。R-02 未整批勾选。
- 下一步：待用户提供账号完成各 G-05；xizhi/SPT 待可核验后再排期。

**P1 批次 CI 验证（2026-09-20）**：`git push origin master` → `95f8bda..27fe326`；GitHub Actions run **35454709464**（name `ci`，head_sha `27fe326…`，event `push`）status `completed`、conclusion **success**。运行链接 https://github.com/seamusic/pusher/actions/runs/35454709464 。本批提交顺序：Domain 常量 → 五 Provider+测试（各自独立提交）→ DI/前端注册 → README/数量 → R-02 文档。

---

## 2026-09-20 复核修复记录

任务编号 / Type / 模式：REV-01～REV-08；关联 F-02、F-03、BUG-01、P0-01、P1-01、P1-04，回归覆盖本期全部已新增通道。

实施状态 / 日期：复核发现的 8 类问题已修复并通过本地自动化 / 2026-09-20。

关联提交或 PR：基于 `00bee0d` 的工作区修改；本次未提交、推送或触发新 CI，前述历史 CI 结果不作为本次修复的验证结果。

官方依据 / 核验日期：[WxPusher 官方文档仓库 HTTP 接口说明](https://github.com/wxpusher/wxpusher-docs#http接口说明)，2026-09-20 复核。标准响应 `data` 是数组，元素包含 uid 或 topicId、code、status；历史实现和测试中的 `data.fails` 结构不符合该协议，原测试通过不足以证明逐目标失败处理正确。

本次改动与涉及路径：

| 复核项 | 问题与修复 | 主要路径 |
|--------|------------|----------|
| REV-01 | WxPusher 按 `data[]` 逐 UID/topicId 检查 code，任一非 1000 返回带目标及 status 的业务错误；缺失、空数组、无效项不视为成功；替换原错误协议样例 | `Channels/WxPusherProvider.cs`、`WxPusherProviderTests.cs` |
| REV-02 | 通道编辑/删除后剩一个共享引用仍保留 Token；删除最后引用才移除。用户清理发生在删库/禁用前，按相同凭证分组排除本用户全部引用，保留其他用户引用 | `Tokens/TokenStore.cs`、`ChannelConfigValidationTests.cs` |
| REV-03 | `channels` 使用 RemoveAllLoggers 关闭会记录 SendKey 路径的默认 HTTP 日志；新增 JSON 通道和 PushMe 的业务错误共用脱敏/截断；HTTP 原因短语、原文和 URL 编码凭证同样脱敏 | `DependencyInjection.cs`、`Channels/HttpChannelHelpers.cs`、本期新增 Provider |
| REV-04 | 消费前检查 AsyncPending，跳过恢复快照与实时入队重叠的已处理 ID；回灌任务自行捕获并立即记录异常；停机取消不标记 Failed，退出时取消并等待回灌 | `Messaging/AsyncMessageWorker.cs`、`AsyncMessageWorkerTests.cs` |
| REV-05 | Server酱 Code 改为可空且必须明确为 0；ntfy 必须有非空 id、event=message 且无 error；Gotify/Pushover 同时返回错误时不接受为成功 | `ServerChanProvider.cs`、`NtfyProvider.cs`、`GotifyProvider.cs`、`PushoverProvider.cs` |
| REV-06 | 新增 JSON 通道解析异常转为不携带原文的 BusinessException；上游超时返回可读业务错误，调用方 CancellationToken 取消保持取消语义 | `Channels/HttpChannelHelpers.cs`、本期新增 JSON Provider、`HttpChannelHelpersTests.cs` |
| REV-07 | URL 拒绝 userinfo；本服务比较使用规范化 scheme/主机/端口和路径边界，阻止显式 :443、主机大小写/尾点绕过，并避免误拒其他域名、端口和路径 | `Channels/HttpChannelHelpers.cs`、`ChannelRegressionTests.cs` |
| REV-08 | 修正 ntfy tags 分支，拒绝非字符串/数组及数组内非字符串项，忽略空白标签；WxPusher mode 存在时必须为字符串。发送路径也校验 ntfy priority 与 WxPusher contentType | `NtfyProvider.cs`、`WxPusherProvider.cs`、`ChannelRegressionTests.cs` |

表中源码位于 `src/MessagePusher.Application/`，应用测试位于 `tests/MessagePusher.Application.Tests/`。日志处理保留应用层的脱敏业务错误，不记录原始响应和敏感请求地址；Token 刷新模型、队列容量 128、单消费者、无自动重试策略不变。重复 ID 修复覆盖正常状态持久化后的单进程场景，不承诺发送成功但落库失败等故障下的严格一次投递。

自动化命令 / 结果：

- 修复前先补回归，在原实现上运行首批针对性用例：**73 项中失败 48、通过 25**，复现协议、共享 Token、日志泄露、回灌重叠、错误响应、URL 和配置类型问题。
- 修复后 `dotnet test MessagePusher.slnx --no-restore -c Release --verbosity minimal -m:1` → **265/265 通过**：Domain 2、Application 252、Api.Integration 11，未跳过。相对原 208 项净增 57 项（Application +52、API +5），同时纠正旧 WxPusher 和 ntfy 响应样例。
- `dotnet build MessagePusher.slnx --no-restore -c Release --verbosity minimal -m:1` → **0 错误**，含 Web/Blazor；3 条既有 SQLitePCLRaw NU1903 警告。测试编译另有既有 CS0642/xUnit1031 警告，本次未扩大范围升级依赖。
- `git diff --check` → 无空白错误。
- `ChannelRegressionTests` 使用真实 DI 注册的 `channels` 日志链及 stub HTTP 验证 SendKey 不进入日志，并覆盖 7 个 JSON Provider 的畸形响应、业务错误脱敏和配置/URL 边界。
- `AsyncMessageWorkerTests` 用同步门控制回灌快照与实时入队重叠，断言只发送、落库一次；另验证回灌失败在停机前已记录。原 0/128/129/256 容量、停机和恢复回归保留。
- 新增 `tests/MessagePusher.Api.IntegrationTests/ChannelErrorTests.cs`：实际走登录、建通道、`/push/root`，验证畸形 JSON、超时、业务失败、缺成功码按现有 API 契约返回 HTTP 200 + success=false + 可读且脱敏的 message；另验证异步推送最终落库 Sent 且上游仅调用一次。

历史证据更正：原 API 集成测试 6 项只覆盖健康检查、状态、登录和 Swagger，历史 R-01 记录“含推送/异步路径”不准确；本次新增 5 项才补齐上述推送覆盖。原 ntfy 测试仅有 id 即视为成功的样例也已修正，新增缺 id、缺 event、错误 event 及带 error 的拒绝断言。

手工联调 / G-01～G-06：上游全部使用 stub/fake，本次无真实通知发送。原各通道 G-05 仍因缺账号、凭证或自建服务待联调；已将 ntfy/Gotify/Pushover/PushMe 含 G-05 的整体验收项恢复为未勾选，保留已完成的自动化子项。P0 的 G-06 说明同步为已随 R-01 完成。

原方案 / README / 数量：原方案 §4.4 明确 WxPusher `data[]` 逐目标成功条件；待办清单更新当前状态、R-02 回归证据及 §9 修复清单。README 和页面继续采用已确定的 24 个 Type 口径，没有新增 Type。

剩余问题 / 下一步：各通道 G-05 真实联调、息知协议核验/实现、WxPusher SPT 核验/实现仍待完成，R-01/R-02 不标为整批验收完成。将来提交本次修改后，再记录对应 CI 结果。
