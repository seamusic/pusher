# 实施记录

按《消息通道扩展实施待办清单》§8.3 模板逐任务追加记录。所有凭证与敏感地址均已脱敏。

---

任务编号 / Type / 模式：PRE-01 / — / —
实施状态 / 日期：已完成（1 项子条目待推送验证）/ 2026-09-19
关联提交或 PR：基线提交 `0179ce1`（master，远程 origin=github.com/seamusic/pusher）
官方依据 / 核验日期 / 上游版本（适用时）：不适用
本次改动与涉及路径：
- 修正 `.github/workflows/ci.yml`：触发路径由失效的 `dotnet/**` 改为 `src/**`、`tests/**`、`MessagePusher.slnx`、`Directory.Build.props`、workflow 自身；移除 `working-directory: dotnet`；构建/测试命令显式指向 `MessagePusher.slnx`（与清单 §8.1 一致）。
自动化命令 / 结果 / 报告位置：
- `dotnet --version` → 10.0.401（.NET 10 SDK 就绪）
- `dotnet build MessagePusher.slnx -c Release` → 0 错误、6 警告（既有，含 NU1903：`SQLitePCLRaw.lib.e_sqlite3` 2.1.10 已知高危漏洞，非本次新增，留待独立处理）
- `dotnet test MessagePusher.slnx --no-build -c Release` → 14/14 通过（Domain 1、Application 7、Api.Integration 6），无既有失败
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非通道任务）
兼容性回归与结果：基线即全绿，无需区分新增回归
原方案 / README / 数量更新位置：无改动
剩余问题 / 阻塞条件 / 下一步：
- CI 触发验收（提交仅触及 `src/**` 的变更并观察 workflow 运行）需在推送后进行；推送需用户确认，暂记为待验证。本地 `ci.yml` 语法与路径已按最终目录修正。
- 基线已实现 16 个 Type 确认：`email, test, corp_app, corp, lark, lark_app, ding, bark, client, telegram, discord, one_bot, group, custom, tencent_alarm, none`（Constants.cs + DI 16 个 Provider 注册一致）。
- 受影响旧行为样本清单（供 F/BUG 任务回归）：API snake_case 契约（JsonDefaults=SnakeCaseLower）、GroupProvider 循环内改写共享 `message.To/Channel`（SignedProviders.cs:355-363，顺序发送）、旧 Provider 经 `HttpChannelHelpers.PostJsonAsync/PostRawAsync` 发送（无 PostFormAsync）、配置更新 Secret 留空保留旧值、TokenStore 仅 test/corp_app/lark_app 三类且 `UpdateChannelAsync` 按旧 Type 早退、历史 `Other` 为标量/模板字符串、异步恢复为先回灌后消费（有界 128、Wait、静态 `_loaded` 守卫、重复 HostedService 注册）。
- 下一步：F-01。

---

任务编号 / Type / 模式：F-01 / group（行为修复）/ —
实施状态 / 日期：已完成 / 2026-09-19
关联提交或 PR：见本次 F-01 提交（基于基线 `0179ce1`）
官方依据 / 核验日期 / 上游版本（适用时）：不适用（内部改造）
本次改动与涉及路径：
- `src/MessagePusher.Domain/Entities/Message.cs`：新增 `Clone()`（全部属性为字符串/值类型，浅拷贝即独立副本）。
- `src/MessagePusher.Application/Channels/SignedProviders.cs`：GroupProvider 循环内不再改写共享 `message.To/Channel`，改为每个子通道 `message.Clone()` 后填写副本；嵌套群组拒绝、数量不一致报错、顺序发送等原有行为保留。
- 新增 `tests/MessagePusher.Application.Tests/GroupProviderTests.cs`（fake 仓储/Provider/作用域）、`tests/MessagePusher.Domain.Tests/MessageTests.cs`（Clone 全字段与独立性）。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release` → 21/21 通过（Domain 2、Application 13、Api.Integration 6）
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用（行为回归由自动化覆盖）
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非新增通道）
兼容性回归与结果：原有 13 项测试全部通过；Group 对外行为差异仅为"原消息不再被篡改"，即本任务目标；PushService 发送后使用的 `message.Id/Link/Async` 不受影响。
原方案 / README / 数量更新位置：无（Type 数量不变）
剩余问题 / 阻塞条件 / 下一步："新增 Provider 不改传入 Message"为编码约束，在 P0 各通道任务中落实并验收。下一步：F-02。

---

任务编号 / Type / 模式：F-02 / —（HTTP 基础能力）/ —
实施状态 / 日期：已完成 / 2026-09-19
关联提交或 PR：见本次 F-02 提交
官方依据 / 核验日期 / 上游版本（适用时）：不适用（内部改造）
本次改动与涉及路径：
- `src/MessagePusher.Application/Channels/HttpChannelHelpers.cs`：
  - 新增 `PostFormAsync`（FormUrlEncodedContent，支持自定义 Header），未新增 `PostTextAsync`；
  - `PostRawAsync` 补读取失败时释放 resp（原泄漏路径）；所有权规则以注释固定：读取失败 helper 释放，正常返回调用方释放；
  - 新增高层 `SendAndReadAsync`：resp 全程由 helper 持有释放；非 2xx 在业务反序列化前转可读 `BusinessException`（`BuildHttpError`：HTML 剥离为单行纯文本、截断 200 字符、按 sensitiveValues 脱敏）；业务 code/纯文本成功语义留给 Provider；无重试；
  - 新增 `OutboundUrlPolicy`（默认仅 HTTPS，`ChannelUrlAllowNonHttps`/同名环境变量开启后允许 HTTP，拒绝非 http(s) 绝对地址，禁止指向 ServerAddress；`WithDefault` 供 Provider 补默认服务器地址）与 `SecretMask`（`Redact` 原样替换凭证、`MaskKey` 日志短掩码）。
- `src/MessagePusher.Application/Channels/SimpleProviders.cs`：CustomProvider 内联 URL 检查迁移至 `OutboundUrlPolicy`（拒绝行为不变，新增拒绝非 http(s) 协议，属有意收紧）。
- `src/MessagePusher.Application/DependencyInjection.cs`：`channels` 客户端超时归口为 30 秒（原默认 100 秒）；按请求仍可用 ct 收紧，新通道不得另设客户端级 Timeout。
- `src/MessagePusher.Application/MessagePusher.Application.csproj`：`InternalsVisibleTo` 开放给 Application.Tests。
- 新增 `tests/MessagePusher.Application.Tests/HttpChannelHelpersTests.cs`（StubHandler/ThrowingStream/FakeOptions）。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release` → 35/35 通过（Domain 2、Application 27、Api.Integration 6）
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非新增通道）
兼容性回归与结果：旧 helper（PostJsonAsync/PostRawAsync）签名与默认序列化行为未变；CustomProvider 拒绝行为回归由 URL 策略测试覆盖；全部既有测试通过。已知差异两处并记录：①CustomProvider 对非 http(s) 协议由放行改为拒绝；②`channels` 客户端超时上限由 100s 收紧为 30s。
原方案 / README / 数量更新位置：无（Type 数量不变）
剩余问题 / 阻塞条件 / 下一步：超时/取消、脱敏、重试口径已固化，P0 各 Provider 直接复用 `SendAndReadAsync`+`OutboundUrlPolicy`。下一步：F-03。

---

任务编号 / Type / 模式：F-04 / —（序列化隔离）/ —
实施状态 / 日期：已完成（与 F-02 同批）/ 2026-09-19
关联提交或 PR：见本次 F-04 提交
官方依据 / 核验日期 / 上游版本（适用时）：不适用（内部改造；协议字段名样例取自原方案 §4.4 WxPusher）
本次改动与涉及路径：
- 新增 `src/MessagePusher.Application/Json/OutboundJson.cs`：出站专用 `JsonSerializerOptions`（无命名策略，字段名由 `[JsonPropertyName]` 决定；响应解析大小写不敏感）。`JsonDefaults` 未改动。
- `src/MessagePusher.Application/Channels/HttpChannelHelpers.cs`：`PostJsonAsync`/`PostRawAsync` 增加可选 `options` 参数（缺省仍为 JsonDefaults，旧调用零影响）；新增 `JsonContent(body, options)` 便利方法供 `SendAndReadAsync` 路径构造出站 JSON。
- 新增 `tests/MessagePusher.Application.Tests/SerializationIsolationTests.cs`。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release` → 40/40 通过（Domain 2、Application 32、Api.Integration 6）
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非新增通道）
兼容性回归与结果：最终发送字节断言 `appToken/contentType/topicIds` 正确、无 `app_token/content_type/topic_ids` 错名；对外 API snake_case 契约（ApiResult、JsonDefaults）与旧 Provider 默认序列化（`chat_id/parse_mode`）回归通过；全局命名策略未被修改。
原方案 / README / 数量更新位置：无（Type 数量不变）
剩余问题 / 阻塞条件 / 下一步：P0-03 PushPlus 与 P1-04 WxPusher 的正式 DTO 随通道任务落地并复用本配置。下一步：F-03。

---

任务编号 / Type / 模式：F-03 / —（配置校验入口）/ —
实施状态 / 日期：已完成 / 2026-09-19
关联提交或 PR：见本次 F-03 提交
官方依据 / 核验日期 / 上游版本（适用时）：不适用（内部改造）
本次改动与涉及路径：
- 新增 `src/MessagePusher.Application/Channels/ChannelConfigValidators.cs`：`IChannelConfigValidator`（按 Type 注册，校验必填项/目标/模式/URL/取值/互斥）、`ChannelConfigValidatorRegistry`、`ChannelTargetPolicy`（动态目标 To 优先；固定目标非空 To 明确业务错误）。
- `src/MessagePusher.Application/Services/ChannelService.cs`：注入 `ChannelProviderFactory` + 校验器注册表；新增与完整更新路径均先合并后校验（Type 必须为已注册 Provider，未知 Type/未实现候选拒绝保存）；同 Type 下 Secret 留空保留旧值，跨 Type 一律取新输入（不盲用旧密钥）；完整更新路径补 Name 长度/保留名校验；statusOnly 单独处理（不要求凭证、跳过配置校验）；返回配置继续清空 Secret。
- `src/MessagePusher.Application/Tokens/TokenStore.cs`：修复 `UpdateChannelAsync` 跨 Type 编辑——旧条目移除与新条目添加相互独立，仅同 Type 沿用"留空保留旧凭证"合并，跨 Type 用新凭证原样构建，防止旧密钥误填充新类型条目。
- `src/MessagePusher.Application/DependencyInjection.cs`：注册 `ChannelConfigValidatorRegistry`。
- 新增 `tests/MessagePusher.Application.Tests/ChannelConfigValidationTests.cs`（ChannelService 校验 8 项、目标策略 2 项、TokenStore 跨 Type 4 项）。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release` → 54/54 通过（Domain 2、Application 46、Api.Integration 6）
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非新增通道）
兼容性回归与结果：API 集成测试全部通过；Secret 留空保留、状态更新不要求密码、返回不回显等旧行为保留。已知有意收紧两处：①完整更新与新增路径此前不校验 Type 合法性，现拒绝未注册 Type；②跨 Type 更新不再沿用旧 Secret。历史 `Other`（custom 模板、corp 的 clientType、tencent_alarm 的 region 等标量语义）未强制改 JSON。
原方案 / README / 数量更新位置：无（Type 数量不变）
剩余问题 / 阻塞条件 / 下一步：各新通道的具体校验器与发送前 `ChannelTargetPolicy` 调用随 P0/P1 通道任务落地。下一步：BUG-01。

---

任务编号 / Type / 模式：BUG-01 / —（异步恢复缺陷）/ —
实施状态 / 日期：已完成 / 2026-09-19
关联提交或 PR：见本次 BUG-01 独立提交
官方依据 / 核验日期 / 上游版本（适用时）：不适用（内部缺陷，原方案 §6.5）
本次改动与涉及路径：
- **复现结果（修复前）**：`Startup_recovery_of_more_than_queue_capacity_completes`（129 条待恢复、有界 128/Wait、fake 仓储/Provider、10 秒有界等待）在修复前失败——`condition not met within 10000ms`，0 条被消费：旧实现 `ExecuteAsync` 先 `await LoadPendingAsync` 再进入消费循环，第 129 条回灌等待队列空位而消费者未启动，永久阻塞。
- `src/MessagePusher.Application/Messaging/AsyncMessageWorker.cs`：回灌改为与消费并发推进（`Task.Run(LoadPendingAsync)` + 消费循环同时进行）；停机时 finally 等待回灌任务，取消（OperationCanceledException）吞掉、其他异常记录日志；移除静态 `_loaded` 守卫（重复注册的伴生补丁，静态状态跨实例/跨测试泄漏）。
- `src/MessagePusher.Application/DependencyInjection.cs`：删除重复的 `AddHostedService<AsyncMessageWorker>()` 注册，单消费者；无多消费者需求，不做显式多实例配置。
- `src/MessagePusher.Application/Messaging/AsyncMessageQueue.cs`：**未改动**——保持有界 128 + FullMode.Wait，未以扩容或无界队列掩盖阻塞。
- 新增 `tests/MessagePusher.Application.Tests/AsyncMessageWorkerTests.cs`。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release` → 61/61 通过（Domain 2、Application 53、Api.Integration 6）。回归覆盖：待恢复 0/128/129/256 条全部进入消费并完成；5000 条回灌期间 StopAsync 不挂起；回灌异常不阻塞后续消费；重启后新实例重新回灌（静态守卫已除）；`Distinct` 断言不因回灌/注册重复发送同一条。
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：不适用（行为由自动化覆盖）
G-01～G-06 完成情况或不适用理由（通道任务）：不适用（非通道任务）
兼容性回归与结果：API 集成测试（含异步发送路径）全部通过；对外行为差异仅为"超过 128 条待恢复消息时不再永久阻塞"与"单一消费者"。
原方案 / README / 数量更新位置：无（Type 数量不变）
剩余问题 / 阻塞条件 / 下一步：本修复不承诺跨进程故障下严格一次送达（进程崩溃时已入队未消费的消息仍靠下次启动回灌恢复），未引入消息中间件。下一步：P0 三通道。

---

任务编号 / Type / 模式：P0-01 / `server_chan` / Turbo + Server酱³（同一 Type，双模式）
实施状态 / 日期：编码与自动化完成，真实联调待条件 / 2026-09-19
关联提交或 PR：见本次 P0-01 独立提交
官方依据 / 核验日期 / 上游版本（适用时）：2026-09-19 复核
- serverchan-sdk README（github.com/easychen/serverchan-sdk）：字段 title/desp，options tags(SC3专用)/short(SC3+SCT)/noip(SCT)/channel(SCT)/openid(SCT)；返回 `{code(0为正确),message,data?}`，data 两版本不同构。
- serverchan-sdk-golang/serverchan.go（官方实现，master）：路由 `^sctp(\d+)t` → `https://<uid>.push.ft07.com/send/<key>.send`，否则 → `https://sctapi.ftqq.com/<key>.send`；非法 sctp 请求前报错；官方 SDK 用 JSON body（`application/json;charset=utf-8`）。
- Turbo 标题上限 32 字符（Server酱 Turbo 文档）；SC3 标题上限未获官方明确数值——标记为未知，SC3 不做本地截断，交服务端判定。
本次改动与涉及路径：
- `src/MessagePusher.Domain/Constants.cs`：新增 `ChannelType.ServerChan = "server_chan"`。
- `src/MessagePusher.Application/Channels/ServerChanProvider.cs`（新增）：`ServerChanKey`（IsValid/IsSc3/ResolveEndpoint/ParseOptions，GeneratedRegex `^sctp(\d+)t`）、`ServerChanProvider`、`ServerChanResponse`、`ServerChanConfigValidator`。
- `src/MessagePusher.Application/Channels/HttpChannelHelpers.cs`：新增共享 `ComposeBody`（Content 空回退 Description，追加 Url 且不重复）、`TruncateUnicode`（按字素簇边界截断，不切代理项）。
- `src/MessagePusher.Application/DependencyInjection.cs`：注册 `ServerChanProvider` 与 `ServerChanConfigValidator`。
- 前端 `src/MessagePusher.Web/Api/Models.cs`（下拉项）、`Components/Pages/ChannelEdit.razor`（Fields：SendKey/channel/高级选项 JSON；Hint：Key 不通用、额度以官网为准、官方链接）。
- 新增 `tests/MessagePusher.Application.Tests/ServerChanProviderTests.cs`（23 项）。
自动化命令 / 结果 / 报告位置：`dotnet test MessagePusher.slnx -c Release --filter FullyQualifiedName~ServerChan` → 24/24 通过；全量 `dotnet test MessagePusher.slnx -c Release` → 84/84（Domain 2、Application 76、Api 6）。覆盖：Turbo/SC3 端点路由、非法 Key 请求前报错（0 次调用）、表单编码与 Content-Type、Content 空回退、Url 不重复追加、Turbo 标题 32 字素边界不切代理项、Turbo AccountId→channel 且 To 覆盖、SC3 拒绝非空 To、short 双版本/tags 仅 SC3、业务 code!=0 报错、空响应报错、非 2xx HTML 转可读错误并脱敏 SendKey、输入消息不被修改；校验器：非法 Key/SC3 带 AccountId/Turbo 带 tags/Other 非法 JSON 均拒绝。
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：**待联调**——无 Turbo 与 Server酱³ 真实账号/凭证，G-05 真实发送、前端创建编辑测试、群组组合、异步发送均未在真实目标验证。
G-01～G-06 完成情况或不适用理由（通道任务）：G-01 已复核（见上）；G-02 完成（Type/Provider/校验器/DI/命名客户端 `channels`）；G-03 完成（下拉/字段/密码输入/Hint，未写死额度）；G-04 完成（自动化捕获最终请求 URL/Header/Content-Type/线上字段，含成功与失败样例，未做真实发送）；G-05 **待联调**（缺账号）；G-06 待 R-01 批次验收后统一更新数量/README。
兼容性回归与结果：既有 Provider 未改；`ComposeBody`/`TruncateUnicode` 为新增共享方法，未触碰旧序列化路径；全量回归绿。
原方案 / README / 数量更新位置：原方案 §4.1；README/数量待 R-01（G-06）统一更新，暂不预增支持数量。
剩余问题 / 阻塞条件 / 下一步：阻塞——缺 Server酱 真实账号，G-05 待联调；表单 POST 与官方 SDK 的 JSON POST 均被端点接受，本实现按待办清单采用表单，真实端点对表单/JSON 的接受度以联调为准。下一步：P0-02 pushdeer。

---

任务编号 / Type / 模式：P0-02 / `pushdeer` / 单模式（表单推送）
实施状态 / 日期：编码与自动化完成，真实联调待条件 / 2026-09-19
关联提交或 PR：见本次 P0-02 独立提交
官方依据 / 核验日期 / 上游版本（适用时）：2026-09-19 复核
- pushdeer.com/official.html：`POST /message/push`，`application/x-www-form-urlencoded`，字段 pushkey/text/desp/type，type 允许 text/markdown/image，默认端点 api2.pushdeer.com。
- easychen/pushdeer `api/app/Http/Controllers/PushDeerMessageController.php`（main）：校验 pushkey/text 必填、desp/type 可空，type 服务端缺省 `markdown`；pushkey 可逗号分隔多 Key。
- easychen/pushdeer `api/app/Http/Helpers.php`（main）：成功 `http_result($content,$code=0)` → `{"code":0,"content":{...}}`；失败 `send_error($error,$code='9999')` → `{"code":"<字符串>","error":"..."}`。故成功判定只认数值 0，错误码可能为字符串。
本次改动与涉及路径：
- `src/MessagePusher.Domain/Constants.cs`：新增 `ChannelType.PushDeer = "pushdeer"`。
- `src/MessagePusher.Application/Channels/PushDeerProvider.cs`（新增）：`PushDeerProvider`、`PushDeerResponse`（`JsonElement Code` 兼容数值/字符串码）、`PushDeerConfigValidator`；复用 `OutboundUrlPolicy.WithDefault/Validate`、`HttpChannelHelpers.ComposeBody/SendAndReadAsync`、`ChannelTargetPolicy.RejectFixedTargetTo`。
- `src/MessagePusher.Application/DependencyInjection.cs`：注册 `PushDeerProvider` 与 `PushDeerConfigValidator`。
- 前端 `Models.cs`（下拉项）、`ChannelEdit.razor`（Fields：PushKey/服务器地址/消息类型下拉；Hint：固定目标、默认端点、可自建、官方链接）。
- 新增 `tests/MessagePusher.Application.Tests/PushDeerProviderTests.cs`（18 项）。
自动化命令 / 结果 / 报告位置：`dotnet test --filter FullyQualifiedName~PushDeer` → 18/18；全量 `dotnet test MessagePusher.slnx -c Release` → 102/102（Domain 2、Application 94、Api 6）。覆盖：默认/自建端点与末尾斜杠、表单编码与 Content-Type、text←Title、desp 回退+Url、type 仅配置时下发、非空 To 请求前拒绝(0 调用)、中文&空格&=符号编码、成功 code0、字符串错误码失败、空响应失败、非 2xx HTML 脱敏 pushkey、http 自建默认拒绝/开关放行；校验器：空 pushkey/非法 type/http 无开关拒绝、text/markdown/空 type 通过。
手工联调环境 / 目标 / 默认或自建端点 / 结果（脱敏）：**待联调**——无 PushDeer 账号与已绑定设备，G-05（默认/自建端点、前端仅 Title+Description 测试消息真实发送）未验证。
G-01～G-06 完成情况或不适用理由（通道任务）：G-01 已复核；G-02 完成（Type/Provider/校验器/DI/命名客户端 `channels`）；G-03 完成（下拉/字段/密码输入/类型下拉/Hint）；G-04 完成（捕获最终请求，含成功与失败样例，无真实发送）；G-05 **待联调**（缺账号）；G-06 待 R-01。
兼容性回归与结果：既有 Provider 未改；复用 F-02 共享方法；全量回归绿。
原方案 / README / 数量更新位置：原方案 §4.2；README/数量待 R-01（G-06）。
剩余问题 / 阻塞条件 / 下一步：阻塞——缺 PushDeer 账号，G-05 待联调；本期不做 Basic Auth（自建鉴权端点不支持），image 类型不在范围。下一步：P0-03 push_plus。
